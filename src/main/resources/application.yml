server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/web1_db?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: ender
    password: 20050614RCD
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0
    timeout: 3000ms
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

# JWT配置
jwt:
  secret: mySecretKey
  expiration: 1800000  # 30分钟

# Gitee OAuth2配置
gitee:
  oauth2:
    client-id: your_client_id
    client-secret: your_client_secret
    redirect-uri: http://localhost:8080/oauth2/register/gitee
    scope: user_info